
fontgrey_str <- "#444444"
green_str <- "#78b45a"
blue_str <- "#51a7f9"
grey_str <- "#7F7F7F"


# We are not really using these at the moment, but it eases future changes
classifier_theme <- theme()
classifier_colours <- theme()

legend_theme <- theme(
  legend.position=c(0.5, 0.95), 
  legend.background=element_rect(fill=alpha('white', 0.0)),
  legend.direction="horizontal")
